name: hsa-snoop - Spell Check

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - 'feat/**'
    paths:
      - '**.md'
      - '.wordlist.txt'
      - '.spellcheck.yml'
      - '.github/workflows/hsa-snoop-spell-check.yml'

permissions:
  contents: read

jobs:
  spell-check:
    name: Spell Check
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v7

    - name: GitHub Spellcheck Action
      uses: rojopolis/spellcheck-github-actions@0.66.0
