# hsa-snoop daemon configuration
# Copy to /etc/hsa-snoop.conf and edit, then restart the service:
#   sudo systemctl restart hsa-snoop

# Extra command-line arguments appended to ExecStart.
# Example: change poll interval, output directory, or force bpftrace mode.
#
# HSA_SNOOP_ARGS="--poll-us 50 --out-dir /var/log/hsa-snoop --format perfetto"
# HSA_SNOOP_ARGS="--all --mode bpftrace --poll-us 20"

HSA_SNOOP_ARGS=""
